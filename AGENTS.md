# 项目协作规范

- 开始工作前阅读 `README.md`、`docs/design.md` 和 `docs/implementation.md`。
- 当前目录是产品项目根目录。不要把界面原型当作已实现的客户端或管理服务。
- 产品界面使用通用术语，不绑定特定组织、注册中心、业务框架或部署用途。
- Windows 客户端采用半透明毛玻璃风格，文字与状态必须清晰可读。
- 普通流程不要求用户配置路由、掩码、转发端口或 NAT 规则；不能自动确定的内容必须明确呈现，不可伪造探测结果。
- 客户端界面与常驻后台服务分离；关闭窗口、手动断开、被踢下线、停用设备的语义必须区分。
- 管理网页和管理 API 仅通过专用网络访问。首次接入需独立引导，不能依赖尚不可访问的管理页面。
- IP 接入必须进行服务端身份校验。不得以跳过 TLS 校验作为正式方案。
- 管理员凭据不得打包到客户端；示例配置不得包含真实加入码、密码、私钥或设备凭据。
- 优先复用成熟组网组件。不得将 NetBird 默认安装等同于已完成本项目的 IP 接入、管理页面隔离或反向访问需求。
- 应用注册和心跳由应用维持；服务映射与注册适配不能产生虚假的健康状态。
- 对本产品创建的网络规则记录归属，支持清理和恢复，不覆盖无关系统配置。
- 测试范围与变更风险对应。界面通过、单机测试通过和真实跨网验收通过必须分别报告。
- 提交或发布前核对第三方许可证。自有许可证未确定前不得对外声称整包采用某一许可证。
