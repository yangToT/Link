# 验证记录

## 2026-09-23：0.2.0-alpha.5 主动退出与路由保留

- 最终发布核对发现主动退出不应复用管理员踢下线的底层节点删除语义。主动退出现在立即清空在线时效并撤销二层接入，由客户端关闭网络进程；保留组网节点和入口路由引用。管理员踢下线、停用和删除仍执行撤销。
- Go 回归验证连续三次主动退出/重连均保留同一底层节点、路由、设备 ID 和凭据，不调用底层删除接口；原有管理员撤销、删除和二层测试通过，vet 通过。
- 云端和当前客户端均更新至 0.2.0-alpha.5；其余 23 项运行服务身份与状态一致。最终版重复实机完全退出/重新连接核验，默认路由、DNS、TUN 状态和设备身份保持，入口恢复为 entry-ready。

## 2026-09-23：0.2.0-alpha.4 退出、设备删除和组件安装修复

- 根据远端用户提供的 SUCCESS 后紧接 FAILED 日志，定位外层 PowerShell 将空/残留 LASTEXITCODE 当作子脚本失败。改用异常传播，真实编排尾部的替身测试覆盖 null、0、1、1060 与真正异常，不安装或卸载系统组件。
- 管理 API 新增设备删除：先撤销二层及组网访问再清理注册记录、目标映射和入口路由。测试覆盖清理失败保留记录后重试、旧令牌失效、自身删除拒绝、未登录拒绝、删除入口撤销依赖设备以及普通上下线保持身份；没有自动删除任何真实设备。
- Windows 完全退出先主动报告断开、完成网络清理并停止自有组件，再停止 LinkAgent；清理失败不退出。窗口关闭提供三种选择，托盘提供完全退出；再次打开可启动后台并复用身份。
- 实机入口首次配置返回 29；核对固定上游 BridgeWin32.c/Admin.c，Windows BridgeCreate 强制要求设备 ID。修复为根据物理 GUID 得到组件标识，并与 BridgeDeviceList 核对后使用。单元测试覆盖正确标识和错网卡拒绝，真实入口进入 entry-ready。
- Go 测试/vet、客户端自测/托盘测试、PowerShell 编排回归、合成网页设备删除和 SSE 测试通过。
- 云端控制服务升级 0.2.0-alpha.4，安装二进制哈希一致；其余 23 项运行服务状态、进程和启动时间未变。当前 Windows 客户端已安装新版，普通用户实际 IPC、私有管理登录和 SSE 验证通过。
- 当前电脑实际调用完全退出：LinkAgent、SEVPNCLIENT、SEVPNBRIDGE 停止，组网子进程不存在，二层恢复日志已清理；再次启动连接后设备 ID 不变，入口恢复为 entry-ready。两阶段默认路由、非 Link 网卡 DNS、TUN 网卡状态与操作前一致。
- 家里电脑仍需安装新版并启用功能；跨电脑 DHCP/桥接、双向业务以及两端代理业务尚未验收。默认路由/DNS/TUN 状态一致不能代替业务验收。

## 2026-09-23：全新源码客户端构建

- 修复源码构建只产出 Link.exe、未自动准备运行依赖的问题。完整客户端构建使用 Python 3.9+ 获取固定版本 NetBird 0.79.0 和 Wintun 0.14.1，校验仓库固定 SHA-256，仅提取明确成员，生成卸载入口并复制许可证。客户端构建不依赖 Go。
- 从 Git 源码导出到全新临时目录，未复制原机 artifacts、私有配置或凭据；使用 Windows PowerShell 5.1 实际下载官方归档并生成完整客户端，文件清单通过。此过程不安装服务或更改网络。
- 使用不可达代理阻断下载后，真实缓存再次完整构建通过；传入不存在的 Python 路径时 CompileOnly 和 Check 仍通过，客户端自测通过。校验失败拒绝替换、缓存离线命中及下载失败保留旧文件测试通过。
- CI 增加完整客户端文件检查，结果需以对应 GitHub Actions 运行为准；当前记录仅确认本机干净源码验证，尚未在另一台实体电脑执行。

## 2026-09-23：中文网卡配置导致二层启动与清理失败

- 实际入口设备恢复记录包含中文网卡名，连接、桥接和虚拟网卡创建标记均为 false。Windows PowerShell 5.1 将无 BOM 的 UTF-8 JSON 按本机 ANSI 编码读取，导致启动及后续清理同时失败。
- 客户端 0.2.0-alpha.3 显式按 UTF-8 读取网络请求、路由归属和卸载记录；清理失败显示未完成的阶段，保留归属记录以便重试，不通过删除记录绕过清理。
- 网络脚本测试改用与 C# 一致的无 BOM UTF-8，并包含中文网卡名。在 Windows PowerShell 5.1 上先复现旧版失败，修复后通过路由归属、TUN 拦截、失败恢复及重复清理测试；均使用内存替身。客户端自测与卸载归属测试通过。
- 服务端代码和协议无变化，无需重启云端；真实双机桥接、DHCP、业务双向访问及两端代理共存仍需单独验收。

## 2026-09-23：0.2.0-alpha.2 升级完成核验

- 当前云端运行二进制与本地 0.2.0-alpha.2 发布构建 SHA-256 一致，服务正常，二层服务端组件已配置；保持路由模式。本次核验无需再次替换或重启服务。
- 本机已安装 Link.exe 与发布构建 SHA-256 一致，卸载登记及后台报告版本均为 0.2.0-alpha.2；后台实际连接成功。此前的旧版回退记录已由此结果更新。
- 安装账户 SID 已配置，当前账户在普通权限进程中通过真实后台命名管道取得状态和管理登录票据，验证权限分离后的 IPC 正常。
- 从本机通过专用网络完成管理首页访问、一次性票据登录、认证状态读取，以及 SSE 首次快照；使用系统证书校验。实际内嵌网页包含新版组件引导和 SSE 代码。
- 本机组件报告就绪，用户已开启本机局域网接入许可；核验保留该选择，没有启用服务端二层模式或创建物理桥接。
- 可选组件 GUI 安装/修复/卸载、双机桥接、双方 TUN 共存和真实业务调用仍需分别验收；不能由本次连接与管理核验替代。

## 2026-09-23：本机二层组件与 0.2.0-alpha.2

- 后续增加客户端“功能与组件”页面，界面渲染通过；基础组件与可选局域网接入分开显示，提供安装/启用/停用/修复/卸载。GUI 使用普通权限，安装任务单独提权，后台管道保留系统/管理员权限并增加安装账户 SID。
- 官方 Windows 安装包固定 SHA-256、Authenticode 签名、资源解压长度限制、提取后二次签名检查全部在真实包上通过；不依赖本机 Python 或 7-Zip。
- Go 测试验证未启用或未准备的成员不获取二层凭据、入口未启用时不能切换模式；管理网页合成 API 测试验证入口提示与禁用条件、SSE 推送和交互。新 GUI 的提权安装、修复及卸载流程尚未在当前电脑实际执行，权限分离也仍待安装后的真实 IPC 验证。

- 本机安装官方签名的 SoftEther Stable 4.44 Build 9807 Client 与 Bridge，分别使用 Link 专属目录、服务和随机管理密码，凭据由 DPAPI 保护。真实 `AccountList`、`CascadeList` 验证通过，均无已建连接；没有创建成员网卡或物理桥接。
- 真实组件测试发现并修复两处调用问题：`/HUB` 会选择 HUB 密码认证，改用服务管理员认证加 `/ADMINHUB:BRIDGE`；带 BOM 的命令文件会返回 117，改为无 BOM UTF-8。启用非交互模式及超时，避免错误密码导致安装一直等待。
- 组件安装前后默认路由、非 Link 网卡 DNS、TUN 网卡状态一致。上游自动安装并绑定了 SeLow 协议驱动；安装日志确认该驱动包为本次创建，已记录清理归属。此结论不等于网卡绑定完全未变，也不等于代理业务已全面验收。
- 卸载替身测试覆盖预先存在驱动、共享 SoftEther 服务、已有驱动包、OEM 包名复用、清理中断重试及驱动包仍在使用。没有在当前办公电脑实际执行卸载。
- Windows 客户端/服务端、Linux 服务端及卸载程序构建通过；Go 测试/vet、客户端自测、PowerShell 网络归属测试、组件密码格式和部署 Python 测试通过。
- 云端控制服务升级至 0.2.0-alpha.2，二进制哈希一致；其余 23 项服务进程与启动时间未变，含新建二层组件。专用二层端点 TLS 证书/地址校验通过，对应公网 TLS 握手不可达，云端仍为路由模式。
- 本机首次升级通过新版后台连接、固定证书健康检查和 Agent SSE，但私有管理检查超时，脚本自动恢复旧客户端。第二次升级的 Windows 权限提示返回用户取消，未执行；因此本机已安装程序仍为 0.2.0-alpha.1，不能把准备好的 0.2.0-alpha.2 安装包记为本机部署成功。
- 双机 DHCP/桥接、双向业务访问、双方代理 TUN、mstsc、断开及完整卸载仍未验收。

## 2026-09-23：二层服务端组件准备

- 从官方稳定版取得 SoftEther 4.44 Build 9807 Linux x64 包，SHA-256 为 `e423e7f94a9f1f97a77b6dd425ba54505f69978964ff97843c90cc619fb70599`。在 `/ROOT/Link` 专属目录构建并通过组件环境自检，构建工具仅解包到该目录，没有安装或升级系统软件包。
- 新增 `link-layer2` 专属服务和 `LINK` 虚拟网络；关闭 SecureNAT、动态 DNS、NAT 穿透、OpenVPN、SSTP、保活外联，没有创建云端物理桥接。
- 初始化脚本首次遇到上游密码散列格式差异时回退配置和防火墙，修正为 SHA-0 后通过真实认证接口测试：GetServerInfo、CreateHub/GetHub、CreateUser/SetUser/GetUser/DeleteUser、EnumSession。禁用用户策略已回读核实，测试账号已删除。
- 本机通过 Link 专用网络访问二层端点完成 TLS 1.3 握手，验证组件证书和地址；公网地址无法完成对应 TLS 握手。云端 `link_guard` 限制 TCP 24448 仅回环/Link0 可达，持久化规则检查通过。
- Link 控制配置已更新，保持原路由模式。除控制服务外，原有 22 个运行服务的进程及启动状态未变。Windows 组件准备和实际双机桥接仍需单独验收。

## 2026-09-23：0.2.0-alpha.1 实际升级

- Linux 控制服务升级至 0.2.0-alpha.1，运行二进制 SHA-256 与本地构建一致；原程序及控制配置/状态已备份，只停止并启动 `link-server`。
- 更新前后其余 22 个运行服务的进程号、状态和启动时间一致，包括 `link-network`、`link-peer`；未重启其他服务或数据库。
- 本机已安装客户端及 LinkAgent 升级，保留原设备身份并重新连接；后台返回新版本和带服务启动标识的状态。
- 通过真实云端验证客户端固定证书的 HTTPS 健康接口及 SSE 状态接收；通过专用网络验证管理登录、状态接口和 SSE 首次快照。
- 实际内嵌网页资源包含 EventSource，旧轮询定时器已移除。公网 `/`、`/api/state`、`/api/events`、`/app.js` 返回 404；私有 API 未登录返回 401。
- 更新前后本机默认路由、DNS 配置及 TUN 网卡状态一致；未据此声称完整代理业务或长时间稳定性已验收。
- Windows 卸载入口已登记，Linux 卸载工具已安装且只读计划通过；没有实际执行卸载。二层组件未启用，文件夹共享仍未实现，双机跨网、真实业务调用及二层/TUN 共存仍待验收。

## 2026-09-23：控制状态改为 SSE 推送（未部署）

- 网页移除每 5 秒全量轮询，客户端新增 SSE 订阅；HTTPS 心跳调整为 15 秒。本机界面的命名管道刷新和服务端 NetBird 状态同步保留。
- Go 测试与 vet 通过：初始快照、状态推送、重连补全、设备/会话撤销、公网管理隔离、连接上限、快照复制和 HTTP/2 长连接保活。
- Windows 客户端构建与自测通过：SSE 分帧和长度限制、状态顺序、本机 TCP 测试端点的鉴权头、断线重连及撤销。未启动真实后台或修改本机网络。
- Playwright 合成 API 检查通过：无 5 秒定时状态请求、推送刷新、断线重连、授权撤销、页面交互及移动布局。
- Windows 客户端、Windows/Linux 服务端及卸载程序构建成功。未替换已安装客户端、未部署云端、未发布新版本；持续代理链路和两台真实设备验收仍待执行。
- 协议选择及升级顺序见 [控制通信说明](communication.md)。

## 2026-09-23：卸载入口与读写共享需求

- 新增客户端/Windows 服务端 `Uninstall.exe` 和 Linux `link-uninstall`，构建成功，打包清单已包含对应脚本。客户端新安装登记 Windows 卸载入口。
- Python 临时目录测试通过：外部单元拒绝、只停止自有服务/删除自有防火墙表、保留数据模式、失败保留现场。
- Windows 临时目录测试通过：只读计划不修改资源、按完整路径筛选服务和进程、用户文件保留、自有实例数据清理、拒绝其他路径的 LinkAgent。服务和进程命令均为替身。
- 当前客户端执行 `-Plan` 只读预览通过；未停止本机服务、未执行真实卸载、未更改云端。Go 测试/vet、Windows/Linux 构建、网页交互检查通过。
- 文件夹共享确认为读写，包括新建、修改和删除；按设备同步网络位置。此项仅记录需求与实现约束，尚未创建真实共享或映射。

## 2026-09-23：二层接入源码预览（未部署）

- 服务端 Go 测试和 vet 通过，Windows `LinkServer.exe` 与 Linux 控制服务交叉构建成功。
- 新增测试覆盖 SoftEther 本机 TLS 证书固定校验、独立设备凭据、失效授权拒绝、指定会话撤销、模式切换前置条件、TUN 地址拒绝、路由重复删除。
- Windows 客户端构建和自测通过：物理网卡歧义处理、无线入口拒绝、重叠网段拒绝、清理失败保留记录、模拟进程重启后继续清理、重复断开不重复删除。
- PowerShell 路由测试通过：成员建卡前出口固定、代理路由拦截、只移除自有规则、失败后恢复。该测试使用内存替身，未执行真实网络命令。
- 本机只读检测在代理 TUN 开启时选择了真实有线接口，并排除 TUN 与蓝牙接口。
- Playwright 合成 API 检查通过：未配置组件时禁用二层切换、模式切换、局域网地址展示、通用服务地址、移动布局、无脚本异常。
- 尚未安装或验证 SoftEther 实际组件、DHCP、物理桥接、双端 TUN、应用注册回调、跨机服务访问、RDP、真实断开还原；未替换当前客户端或云端进程，未发布新版本。

## 2026-09-23：Windows 部署与客户端 0.1.0-alpha.2

- 在一台真实 Windows 电脑安装后台服务、驱动和实例 CA，成功接入云端专用网络。
- 本机访问私有管理网页、一次性凭据登录、认证后的管理 API 均返回 200，使用系统证书校验。
- 后台服务设置为自动启动、自动连接；实际重启 LinkAgent 后恢复同一设备身份与连接。未重启 Windows，不能据此替代系统启动验收。
- 客户端嵌入多尺寸应用图标，使用系统托盘组件。`Link.Check.exe --tray-test` 验证图标加载、最小化收起、普通/最大化窗口恢复、关闭收起及退出时清理托盘。
- 原有 TCP 半关闭传输、IPC、地址验证、Job 子进程退出自测仍通过。
- 尚需第二台 Windows 设备验证互连、资源访问与真实应用调用。

## 2026-09-22：0.1.0-alpha.1

日期：2026-09-22。

## 通过

- Go 测试及 `go vet`：公网管理隔离、加入码单次使用、底层失败不消耗加入码、令牌散列存储、停用拒绝重连、暂停不自动恢复、会话来源绑定、CSRF、映射端口唯一分配、过期心跳不显示在线。
- Windows 构建与无管理员自测：地址校验、网络分类、IPC 序列化、真实本机 TCP 中转 1 MiB 数据及半关闭响应完整性。
- 正式 Web 前端 Playwright 检查：设备列表、加入码对话框、应用映射、启动参数、375px 布局、无 JavaScript 错误。API 为合成测试数据。
- Windows 客户端实际校验云端 TLS；错误的 CA 指纹被拒绝。内核 Job Object 的子进程退出约束通过自测。
- 外部 UDP 3478 STUN 收到合法绑定响应。
- WPF 离屏布局渲染；未验证实际系统毛玻璃合成。
- Linux 原生组网组件、控制服务和专用管理节点真实启动，无 Docker。
- 外部网络校验实例 CA 后访问 HTTPS：`/health` 为 200；`/`、`/api/state`、`/api/setup`、`/app.js` 为 404。
- 云端通过专用网络地址访问内嵌网页为 200，未授权私有 API 为 401。
- 部署前后原有六项服务的进程号、状态和启动时间完全一致。

## 未验收

- 当前 Windows 会话没有管理员权限，尚未验证驱动安装、后台服务安装、自启、证书安装和卸载恢复。
- 两台真实 Windows 跨网络连接、RDP、原地址资源访问。
- Windows 入口反向转发、实际应用注册与调用。
- 已有数据流的撤销时限、断电/断网恢复、P2P/中继切换与吞吐。
- 重叠网段消歧、回环服务桥接、UDP 映射尚未实现。
- Windows 完整组网服务端安装：EXE 当前仅包含控制面与网页。

GitHub SSH 与 API 身份均已验证，公开仓库为 https://github.com/yangToT/Link。发布结果以 GitHub Releases 的实际资产为准。


## 0.2.0-alpha.6 卸载、进度与设备标识

- 隔离客户端卸载：真实测试进程从解压目录运行，验证退出和文件移除；文件锁造成失败时不报成功、保留身份，解除锁后重试成功。模拟网络恢复失败时保留程序与恢复材料。系统命令用替身，不等于真实驱动卸载验收。
- Windows 服务端卸载：服务/进程命令替身验证归属和清理结果，保留同目录用户文件。
- 原生进度窗：脚本输出逐阶段刷新，执行中关闭被阻止；非零退出和缺少完成标记都失败，只有完整成功才结束进度条。
- 客户端自测与管理网页合成 API 验证局域网接入开关、已取得地址、入口就绪、异常与离线状态；管理状态通过既有 SSE 更新。
- 未卸载当前生产使用中的 Windows 客户端或远程服务；两台真实 Windows 的卸载及跨网业务验收仍需单独执行。


## 0.2.0-alpha.7 客户端操作反馈

`Link.Check.exe --interaction-test` 验证真实 WPF 样式触发、禁用状态、异步等待、重复操作拦截、成功与失败反馈和控件恢复；生成合成操作截图用于检查。网络协议与服务端没有改动。该检查不调用安装、卸载或真实网络修改。

## 0.2.0-alpha.8 客户端（2026-09-23）

- 家里设备实际上报 NicCreate 退出码 32，历史组件安装日志本身成功。按 SoftEther Stable 4.44-9807 源码确认 Windows 允许 VPN、VPN2 至 VPN127；现改为选择空闲合法名称，GUID 归属保护及失败恢复记录。
- 公司本机执行官方签名驱动的隔离测试：创建未连接 VPN127，记录 GUID，禁用并删除。没有创建连接/桥接；清理后默认路由、DNS、网卡清单均与基线一致。
- 客户端自测：版本排序、预览/稳定通道、降级/草稿拒绝、资产地址、校验和冲突、延期/跳过、压缩包越界拒绝；真实 GitHub 发布列表读取成功。
- WPF 检查更新/重启选项、异步按钮重复点击保护；原按钮反馈测试通过。更新可用及待重启画面已渲染检查。
- 网络脚本替身检查：合法名称、GUID 替换拒绝、网卡不存在时幂等清理、原有端点路由和 TUN 检查通过。进度弹窗和组件驱动清理回归通过。
- 本机真实安装事务：人为使新版本健康检查失败，确认原程序哈希和旧后台版本恢复；随后升级 alpha.8，确认安装哈希与后台版本、设备身份和连接偏好保留。默认路由、DNS、TUN 基线一致；公开固定证书 TLS、agent SSE、私有管理登录/状态/SSE 恢复。
- 边界：测试的临时虚拟网卡未加入远端网络；家里设备的取址及真实跨网应用访问仍待升级后验证。自动更新的真实未来发布端到端操作链尚未验证；不得以安装事务测试替代该验收。服务端本次没有升级或重启。

## 0.2.0-alpha.9 成员连接修复（2026-09-23）

- 修复 Stable 4.44/9807 不支持的 `AccountDetailSet /DISABLEUDP` 参数。成员生产配置流程抽出复用，回归检查认证、证书固定校验和连接顺序，以及 Stable 参数兼容性。
- 云端专用 Hub 的 `DisableUdpAcceleration` 已设置并回读为 1；仅调整该选项，未重启任何服务。初始化脚本同步设置；维护脚本覆盖选项保留、重复执行、未知版本拒绝和验证失败恢复测试。
- 本机真实 Windows 测试调用生产成员配置方法：临时签名网卡、账号创建、口令、服务器证书设置及启用、AccountDetailSet、AccountConnect 全部成功。连接云端独立测试 Hub，报告 `Connection Completed (Session Established)`、TCP 24448、UDP acceleration supported/active 均为 No。
- 测试 Hub 未桥接办公网、未启用 NAT/DHCP。测试结束删除本机临时连接和网卡、云端临时 Hub；默认路由、DNS、网卡清单与测试前完全一致。
- 完整 Windows 包构建、自测、WPF 操作反馈、网络保护脚本检查通过。家里设备 DHCP 取址、双方代理并行运行及真实跨网双向业务访问仍未验收。
- 发布后，安装目录中的 alpha.8 实际检测并下载 GitHub alpha.9 发布包，生产下载代码通过 HTTPS、SHA256SUMS 和 GitHub digest 校验。客户端自身的 Prepare/Apply/Watch 任务随后完成，受保护任务记录为 SUCCESS；worker 哈希匹配旧 alpha.8，payload 与安装目录哈希均匹配发布的 alpha.9，新窗口已启动。
- 升级后真实后台报告 alpha.9 且已连接；固定证书公开健康接口、agent SSE、私有管理登录/状态/管理 SSE 检查均成功。发布提交 68bb66e 的分支和标签 CI 均通过。
- 验证脚本第一次把 PowerShell 文件附加属性误序列化成下载路径，准备阶段因路径过长失败且未覆盖旧版本；修正脚本时客户端自身已完成升级，重复脚本因旧窗口退出停止。这两次辅助脚本失败不作为产品自动更新通过证据；通过结论来自实际客户端更新任务与升级后健康检查。升级期间没有取得完整的前后网络快照对比，不能用前述隔离连接测试的快照替代。
